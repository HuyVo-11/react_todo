import './TodoShared.scss';
import { TodoContext } from './index';

const TodoCompleted = () => {
    return (
        <div>
            vc đã hoàn thành
        </div>
    );
};

export default TodoCompleted;
